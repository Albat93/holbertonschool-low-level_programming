#include <stdio.h>

int main(void);
/**
 * main - Entry point
 * Return: Always 0 (Success)
*/

{

char alphabet;
for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
{
putchar(alphabet);
}
putchar('\n');
return (0);
}
