#include "main.h"

/**
 * main - Entry point
 * Return: Always 0 (Success)
*/
int main(void)

{

void print_alphabet(void)
	char letter;
for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
{
_putchar(alphabet);
}
_putchar('\n');
return (0);
}
